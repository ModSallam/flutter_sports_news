import 'package:flutter/material.dart';

class FixturesMatchScreen extends StatelessWidget {
  const FixturesMatchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Padding(
        padding: const EdgeInsets.all(20.0),
        child: ListView(),
      ),
    );
  }
}
